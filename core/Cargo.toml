[package]
name = "tidytrail-core"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "Directory scanning, squarified treemap layout, and trash-backed deletion for TidyTrail Desktop."

[dependencies]
serde = { version = "1", features = ["derive"] }
trash = "5"

[dev-dependencies]
tempfile = "3"
