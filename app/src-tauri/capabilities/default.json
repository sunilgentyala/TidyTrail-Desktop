{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Least-privilege capabilities for the main TidyTrail Desktop window: core IPC/events, the native folder picker, and native message boxes. Nothing else from the dialog plugin (save/ask/confirm) and no shell/opener access.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "dialog:allow-message"
  ]
}
